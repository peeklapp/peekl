name: run-tests
on: [push]
jobs:
  tests:
    runs-on: ubuntu-latest
    container: golang:1.26.0-trixie
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: 1.26
      - name: Run golang tests
        run: go test -coverprofile=coverage.out ./...
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: 1.26
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12

